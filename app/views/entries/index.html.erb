<h2><%=l @date, :format => :long_ordinal%></h2>

<% unless @entries.empty? %>
  <table id="tracker">
    <thead>
      <tr>
        <th>Food</th>
        <th class="last">Points</th>
      </tr>
    </thead>
    <tfoot>
      <tr>
        <td>Total</td>
        <td class="last"><%=@entries.sum(:points)%></td>
      </tr>
    </tfoot>
    <tbody>
      <%= render @entries %>
    </tbody>
  </table>
<% end %>

<%= form_for @entry do |f| -%>
  <fieldset class="inputs">
    <ol>
      <li class="required">
        <%= f.label :name, 'Food' %>
        <%= f.text_field :name %>
      </li>
      
      <li class="required">
        <%= f.label :points, 'Points' %>
        <%= f.text_field :points %>
      </li>
    </ol>
  </fieldset>
  
  <fieldset class="buttons">
    <%= submit_tag 'Add' %>
  </fieldset>
<% end -%>
