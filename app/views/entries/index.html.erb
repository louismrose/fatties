<% unless @entries.empty? %>
  <table>
    <thead>
      <tr>
        <th>Entry</th>
        <th class="last">Points</th>
      </tr>
    </thead>
    <tfoot>
      <tr>
        <td>Total</td>
        <td><%=@entries.sum(:points)%></td>
      </tr>
    </tfoot>
    <tbody>
      <%= render @entries %>
    </tbody>
  </table>
<% end %>

<%= form_for @entry do |f| -%>
  <fieldset class="inputs">
    <ol>
      <li class="required">
        <%= f.label :name, 'Food' %>
        <%= f.text_field :name %>
      </li>
      
      <li class="required">
        <%= f.label :points, 'Points' %>
        <%= f.text_field :points %>
      </li>
    </ol>
  </fieldset>
  
  <fieldset class="buttons">
    <%= submit_tag 'Add' %>
  </fieldset>
<% end -%>
